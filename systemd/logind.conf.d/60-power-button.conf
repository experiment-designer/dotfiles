[Login]
# Ignore accidental taps, but perform an orderly shutdown after systemd's
# five-second long-press threshold. The firmware may still force power off if
# the button is held beyond its own emergency cutoff.
HandlePowerKey=ignore
HandlePowerKeyLongPress=poweroff
