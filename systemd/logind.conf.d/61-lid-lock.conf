[Login]
# Closing the lid only locks the session (xss-lock picks up logind's Lock
# signal and runs xsecurelock); it never suspends, so long-running jobs keep
# going. Suspend manually with `systemctl suspend` when wanted.
HandleLidSwitch=lock
HandleLidSwitchExternalPower=lock
HandleLidSwitchDocked=lock
